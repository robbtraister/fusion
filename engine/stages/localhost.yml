events:
  - http: ANY /
  - http: 'ANY /{proxy+}'

skipCacheInvalidation: false
# cacheInvalidationRegex: '^(?!/workdir/engine/bundle/(?!node_modules/))'
cacheInvalidationRegex: '(/node_modules/|^/workdir/engine/(?!bundle))'
