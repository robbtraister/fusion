'use strict'

module.exports = require('express-handlebars')

// if templates are pre-compiled...

// module.exports = options => (fp, data, cb) => {
//   const render = require(fp)
//   cb(null, render(data))
// }
